import { HighlightWhiteDirective } from './highlight-white.directive';

describe('HighlightWhiteDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightWhiteDirective();
    expect(directive).toBeTruthy();
  });
});
